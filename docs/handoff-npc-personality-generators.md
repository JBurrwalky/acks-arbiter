# Build Handoff — NPC Personality Generators (Relationships & Knowledge)

**For:** Claude Code (build agent)
**Spec:** `generation/gdd-npc-personality.md` (last updated 2026-06-08; core-layer revision note 2026-06-14) — the authoritative design. §5 (Relationship Network Generation) and §6 (NPC Knowledge System) are the sections this handoff builds; §11.1 specs the file layout. The GDD is the source of truth for any table shape or rule not repeated here.
**Status of design:** §5 and §6 are drafted and detailed (procedures, data shapes, validation rules) but were **never implemented** — the GDD's own §13 revision history states the twelve-axis CORE shipped 2026-06-14 and explicitly lists relationships (§5) and knowledge (§6) as **NOT YET BUILT (deferred)**. This handoff is not new design — it crystallizes the existing §5/§6 text into sequenced, verifiable build sessions. Where §5/§6 are ambiguous or silent on something a build session needs, this handoff does **not** invent an answer — see §9 "Open Questions / Rulings Needed from Jedidiah" below.
**Author:** Advisor (design). **Date:** 2026-07-07.

---

## 0. How to use this document

This is a **four-session build**: RG-1 (relationship generator + table), RG-2 (knowledge assigner + table + rumor-pool linkage), RG-3 (wiring both into settlement stocking / NPC materialization), RG-4 (feeding both into `DialogueContext`). Do the sessions in order — RG-1 and RG-2 are independent of each other and could theoretically run in parallel, but RG-3 depends on both, and RG-4 depends on RG-3. **Recommended order is still sequential** (RG-1 → RG-2 → RG-3 → RG-4) so a single build agent's context carries forward the schema decisions cleanly; do not start RG-3 from a fresh session without both RG-1 and RG-2 landed.

Each session below has: **Goal · GDD refs (§ + line numbers against the read verified 2026-07-07) · Files · Acceptance bar · Model · a paste-ready prompt.** Paste one session's prompt into a fresh build session, let it go green, then move on.

**Do not redesign §5/§6.** They are detailed, deliberate procedures (relationship-count bands from axis values, knowledge category tables, rumor-pool linkage). If you find a genuine ambiguity — a case §5/§6 doesn't cover, or a schema decision the GDD leaves implicit — **stop and consult §9 of this document**, and if it isn't answered there, flag it for Jedidiah rather than guessing. Every ACKS-adjacent claim (there are few in this GDD; it's Authority: PROJECT-DESIGNED per its own header) still routes through `acks-raw-lookup` if a build session touches loyalty/morale mechanics tangentially (§5.2's `family`/`employer` "mechanical effect" column references the ACKS loyalty system — cite it, don't reinvent it).

**Correction to this handoff's originating brief, verified against the codebase 2026-07-07 (read this before starting RG-3/RG-4):** the task that produced this document assumed `StrategicDisposition`/`RulerProfile` (GDD §8) was *also* still unbuilt, alongside §5/§6. **That is stale.** §8 shipped in a later session (`gdd-ruler-ai.md`-driven, referenced 2026-07-01 in `docs/coding_conventions.md` §89) as `engine/shared_types/strategic_disposition.gd`, `engine/shared_types/ruler_profile.gd`, and `engine/subsystems/realm_ai/strategic_disposition_builder.gd` — which **supersedes** GDD §11.1's older file-placement plan of putting `strategic_disposition.gd` in `engine/subsystems/generation/npcs/`. Do not build §8 from this handoff; it already exists. More importantly: **`strategic_disposition_builder.gd`'s own docstring names the relationship-generator gap as a live, active blocker** — the §8.3 revenge-bump term ("+0.5 when motivation == revenge and the target is the revenge subject") is currently dormant because it needs an `NpcNpcRelationship` row identifying who the "revenge subject" is, and nothing populates that today. **RG-1 (this handoff) is a direct unblock for a shipped feature, not just GDD-completeness work.**

---

## 1. Shared preamble (every session begins with this)

Run the **Build Session Protocol** from `CLAUDE.md` (build-log navigator, design brief, conventions navigator, raw-lookup for rules; append a `build_log.md` entry at session end, `--lint` after).

**Hard constraints (do not violate):**

- **Determinism.** Relationship and knowledge generation are seeded and reproducible from (world state + seeded RNG), exactly like the existing axis sampler (`PersonalityAxisSampler.make_rng` in `npc_personality_generator.gd:38`). No LLM call anywhere in RG-1/RG-2/RG-3. RG-4 only *reads* already-generated data into a dialogue context; it still makes no LLM call (LLM narration is a downstream consumer, out of scope here).
- **Banker's rounding** everywhere a value rounds (relationship-count banding, if any fractional midpoint arises; none is currently visible in §5.3's banding but keep the discipline live).
- **No new autoloads.** New services are `RefCounted` classes with `class_name`, following `NpcPersonalityGenerator`'s exact pattern (`class_name X extends RefCounted`, instantiated by callers, static data banks cached via a lazy `_ensure_data()`/`_load()` idiom — see `docs/coding_conventions.md` line ~3892, citing `ClassTemplateRepository` as "a non-autoload `class_name` service mirroring `EquipmentCatalog` / `MagicItemCatalog`"). Land the new files in the existing `engine/subsystems/generation/npcs/` folder per GDD §11.1 — do **not** create a new subsystem folder for this.
- **SQLite is ground truth.** Migrations live in `db/migrations/`, sequential. **Verified 2026-07-07: the latest migration is `186_armies_provenance.sql`.** The next migration for this arc starts at **`187_`**. Non-destructive; godot-sqlite calling conventions per CLAUDE.md (`query()` no args / `query_with_bindings(sql, array)`, never a second arg to `query()`). New tables MUST be added to `CampaignRepository`'s campaign-purge cascade — verified at `engine/autoloads/campaign_repository.gd`: the direct-scope list is `_SCOPE_DIRECT_CAMPAIGN` (array starts line 5329; `"factions"` sits in it at line 5335 as the nearest precedent for a table that is NOT itself campaign-scoped by a column but is referenced by campaign-scoped rows — read the array's full membership before adding, most rows there DO carry `campaign_id` directly), and the `_via` chain (starts ~line 5390, e.g. `e.append({"table": "faction_memberships", "via": _via("faction_id", "factions")})` at line 5408) for tables that key off a parent instead of `campaign_id` directly. A new NPC-relationship or NPC-knowledge table keyed by `npc_id`/`character_id` (not `campaign_id` directly) belongs in the `_via` chain, keyed off `characters` (pattern: `_via("npc_id", "characters")` or equivalent — verify the exact helper signature of `_via()` before use, it is not fully quoted here).
- **After adding new `.gd` files:** run the `--import` pass before the headless test runner (commands in CLAUDE.md).
- **Naming per CLAUDE.md:** PascalCase classes, snake_case files, past-tense snake_case signals, plural snake_case tables.
- **Test baseline:** re-establish the current baseline with the project's test runner (`run_tests.sh`/`.ps1` if present, else the headless `--path . res://tests/test_runner.tscn` command in CLAUDE.md) before your first change in each session; definition of done everywhere is **net-zero new failures** against that freshly-established baseline (do not reuse a stale baseline number from a different handoff — establish it fresh, the exact pass/fail count was not re-verified for this document and will have moved since the faction-framework handoff's 2026-07-03 baseline).
- **Naming collision to avoid — read before touching `DialogueContext` (RG-4 especially).** `generation/gdd-npc-dialogue.md` §4.3 (line 180) already names a type `NpcRelationship` — but that type is **PC↔NPC attitude/ledger** (attitude, attempt counter, disposition ledger; a dialogue-system concept, not yet built either, tracked in that GDD's own future work). This handoff's §5 relationship generator produces a **different concept: NPC↔NPC social-network edges** (family/friend/rival/enemy/etc. between two NPCs, per GDD-personality §5.2/§5.4). **Do not name the new shared type `NpcRelationship`** — it will collide with the dialogue GDD's planned type of the same name for a different shape. This handoff's shared type is called `NpcNpcRelationship` throughout (placeholder name — see §9 Open Question 1 on whether Jedidiah wants a different final name before RG-1 lands the migration).

**What already exists (verified 2026-07-07 — ground every file reference in this handoff against these facts, they supersede any stale assumption):**

- `engine/subsystems/generation/npcs/` currently contains exactly four files: `axis_sampler.gd`, `npc_personality_generator.gd`, `personality_axes.gd`, `personality_mock.gd`. `relationship_generator.gd` and `knowledge_assigner.gd` are confirmed absent — searched the entire `engine/` tree (including all `.claude/worktrees/` copies), zero matches.
- `engine/shared_types/npc_personality.gd` — `class_name NpcPersonality extends RefCounted`. Fields (verified, lines 18-37): `SCHEMA_VERSION`, `tier`, `axes: Dictionary`, `sampled_axes: Array`, `motivation_primary`, `motivation_secondary`, `distinctive_feature`, `personality_summary`, `speech_notes`, `disposition: int`. **No relationship or knowledge array fields exist on this type, not even empty placeholders.** Its own docstring (lines 14-16) states relationships/knowledge "are separate, deferred subsystems. A future session adds them as their own tables keyed by npc_id." `to_dict()`/`from_dict()` exist (lines 64-113) with the standard `.get(key, default)` resilience pattern per `docs/coding_conventions.md` §7.2.
- `characters` table (`db/schema.sql:47`) has a `personality TEXT NOT NULL DEFAULT '{}'` column (JSON blob, confirmed). **No relationship or knowledge columns or tables exist anywhere in `db/schema.sql` or any file in `db/migrations/`** — grepped for both terms project-wide; the only hits are unrelated (liege/vassal comments, hex-map parent/child links, dungeon TEXT-key relationships, migration precedent for OTHER systems).
- `ClassedNpcBuilder` (`engine/subsystems/characters/classed_npc_builder.gd`) attaches personality inside `build_classed_npc()` at lines 220-234, calling `_ensure_personality_generator().attach_to_character(character, {...})` with `culture_id`/`culture_name`/`role`/`settlement_name`/`seed_key` opts. This runs near the end of the function (before the return around line 236), after equipment/spells resolve, on an in-memory `CharacterData` bundle — `persist()` (line 249) is the separate DB-write step that follows. **No `generate_relationships`/`generate_knowledge` opts exist; grep for "faction" in this file returns zero hits.**
- `NpcRulerGenerator` (`engine/subsystems/domains/npc_ruler_generator.gd`) calls personality generation at lines 133-137 (`role: "ruler"`) and **already** calls `StrategicDispositionBuilder.build_and_persist_for_character()` at lines 161-165 (confirming §8 is live, per the correction above).
- `BaselineNpcStocker` (`engine/subsystems/settlements/baseline_npc_stocker.gd`) is the **Tier-B batch settlement-stocking entry point** (GDD §10.1). Its static `stock_poi()` (line 115) fires from `EventBus.poi_emerged` (signal declared `engine/autoloads/event_bus.gd:2032`) via `_on_poi_emerged` (lines 103-104), builds personality JSON directly via `NpcPersonalityGenerator.new().generate({...})` (not `attach_to_character`, since baseline NPCs aren't `CharacterData` objects yet at that point) for the head NPC (line 208) and adherents (line 237), inserts character rows with personality baked in (lines 192-241), then calls `CampaignRepository.update_settlement_poi_baseline_head(poi_id, head_id)` and emits `EventBus.poi_stocked.emit(poi_id, head_id)` (`event_bus.gd:2037`, lines 244-245 in the stocker). **No listener currently exists on `poi_stocked` for anything relationship/knowledge-related.**
- **Faction tables already exist and are queryable, but are completely unwired into the personality/stocking pipeline.** `factions` (`db/schema.sql:1465`, columns: `id, campaign_id, name, alignment, faction_type, home_domain_id, leader_npc_id, parent_faction_id, description, created_at`) and `faction_memberships` (`db/schema.sql:1479`, PK `(faction_id, npc_id)`, plus `role`) were created by `db/migrations/025_reputation_system.sql` — long before the personality core. But `classed_npc_builder.gd` and `baseline_npc_stocker.gd` both grep to zero hits for "faction," and `axis_sampler.gd`'s `_faction_shift()` helper (lines 85-90) is an explicit no-op passthrough. **GDD §5.3's "Same faction: bias toward friend/employer/patron/co-conspirator" and "Rival faction: bias toward rival/enemy" rules require new query code against `faction_memberships`/`factions` — the tables are there and populated (wherever factions exist in a campaign), nothing currently reads them for this purpose.**
- `generation/gdd-quest-rumor-system.md` (951 lines, Status: Draft, last updated **2026-03-28** — this predates the personality GDD's own 2026-06-08 rework, it is an established prior GDD, not something newly concurrent) already specs a `RumorEntry.knowledge_category` field (line 72, quoting: `# From gdd-npc-personality.md §6.2: "local", "professional", "political", "criminal", "religious", "military", "dungeon", "personal", "historical"`) and its own §2.4 rumor-distribution rule requires "The NPC has a matching knowledge_category... OR the rumor's knowledge_category is 'local'" (lines 120-122). **This means the rumor system's own distribution logic is currently unimplementable for anything but the hardcoded 'local' case, because no NPC has a populated knowledge_category set until `knowledge_assigner.gd` exists.** RG-2's linkage work is a direct unblock for `gdd-quest-rumor-system.md`, not a nice-to-have.
- `generation/gdd-npc-dialogue.md` (Status "Draft v0.6", last updated 2026-07-03) already names both consumer slots in its `DialogueContext` struct (§4.3, lines 165-191): `personality: Dictionary` (line 182, "from characters.personality") and, inside `hooks` (line 185), `rumor_pool_ids: Array, knowledge_entries: Array`. §9.1 (lines 502-506) already specs how `KnowledgeEntry.willingness_to_share` gates the `ask_question` move (`freely` at Neutral+, `if_trusted` needs Friendly, `if_paid` spawns a negotiation, `never` yields refusal-or-lie). **These slots are named and speced on the consumer side already — RG-4 is filling in a hookup that the dialogue GDD is already written expecting to exist.**

---

## 2. Session map

| Session | Deliverable | Depends on | Model |
|---|---|---|---|
| **RG-1** | `npc_npc_relationships` table + `NpcNpcRelationship` shared type + `RelationshipGenerator` service (sociability banding, type-selection biases, network validation) + repository CRUD + purge-cascade registration | — | Sonnet |
| **RG-2** | `npc_knowledge_entries` table + `KnowledgeEntry` shared type + `KnowledgeAssigner` service (baseline/role/relationship/rumor-pool knowledge assignment) + repository CRUD + purge-cascade registration + `gdd-quest-rumor-system.md` linkage | — | Sonnet |
| **RG-3** | Wire both generators into `BaselineNpcStocker`/settlement-stocking completion (settlement-wide pass, not per-POI) + `ClassedNpcBuilder` opts + Tier-C→B/A promotion fill-in | RG-1, RG-2 | Sonnet; flag the settlement-wide-pass timing seam `[NEEDS-OPUS-REVIEW]` if it touches `EventScheduler` ordering |
| **RG-4** | Populate `DialogueContext.personality`/`hooks.knowledge_entries`/`hooks.rumor_pool_ids` (relationships surfaced via `relevant_relationships` per GDD-personality §9.2's `NPC Context Package`) from the new tables | RG-3 | Sonnet |

**Global definition of done:** on a freshly settlement-stocked campaign — every Tier B+ NPC has 2-5 relationships per the §5.3 sociability bands (withdrawn-equivalent NPCs 1-2, gregarious-equivalent 4-5, otherwise 2-3), no Tier B+ NPC is completely isolated, every faction leader has ≥1 relationship with every other faction leader in the same settlement, the thieves' guild master (where one exists) has ≥1 co-conspirator and ≥1 enemy; every resident NPC has baseline local knowledge scaled by tier (Tier C 3-5 facts, Tier B 8-12, Tier A 15+) plus role-based and relationship-based knowledge per §6.3; each NPC's rumor pool (2-5 rumors) is populated consistent with `gdd-quest-rumor-system.md`'s distribution rule reading a real `knowledge_category`; `StrategicDispositionBuilder`'s revenge-bump term becomes livable (a `revenge`-motivated NPC has an identifiable revenge-subject via an `enemy`-typed `NpcNpcRelationship` row — confirm with Jedidiah per Open Question 4 below whether RG-1 should wire this bump itself or merely unblock it for a follow-up session); `DialogueContext.personality`/`hooks.knowledge_entries`/`hooks.rumor_pool_ids` are populated from real data on a live dialogue session; determinism holds (same seed + same world state → byte-identical relationship/knowledge generation); suite net-zero; `build_log.md` entry per session.

---

## 3. Session RG-1 — Relationship table, type, and generator

**Goal.** Give every Tier B+ NPC a generated social network per GDD §5, stored durably and queryable, unblocking the dormant `StrategicDisposition` revenge-bump term.

**GDD refs.** `generation/gdd-npc-personality.md` §5.1 (when generated, lines 315-317), §5.2 (relationship types + mechanical effect table, lines 319-332), §5.3 (generation procedure — sociability bands from axes, type-selection biases, network validation, lines 334-368), §5.4 (data shape, lines 370-381).

**Files.**
- Migration `db/migrations/187_npc_npc_relationships.sql` (verify 186 is still the latest immediately before writing this — another session may have landed since 2026-07-07). Table name **`npc_npc_relationships`** (the doubled "npc_npc" is deliberate — distinguishes from the dialogue system's future PC↔NPC relationship table; confirm this naming choice against Open Question 1 before committing). Columns per GDD §5.4 verbatim: `npc_a_id TEXT NOT NULL REFERENCES characters(id)`, `npc_b_id TEXT NOT NULL REFERENCES characters(id)`, `type TEXT NOT NULL CHECK(type IN ('family','friend','rival','enemy','employer','patron','debtor','mentor','co-conspirator','unrequited'))` (the ten types from §5.2), `strength INTEGER NOT NULL CHECK(strength BETWEEN 1 AND 5)`, `mutual INTEGER NOT NULL DEFAULT 1` (bool as 0/1 per `docs/coding_conventions.md` §7.2 serialisation rule), `public INTEGER NOT NULL DEFAULT 1`, `note TEXT NOT NULL DEFAULT ''`. Decide + document a directionality convention (is `unrequited` encoded via `mutual=0` plus a fixed a→b admirer-first ordering, or does it need an explicit `admirer_is_a` style column? — GDD §5.4 doesn't say; flag if genuinely ambiguous, see Open Question 2). No UNIQUE constraint is specified by the GDD across `(npc_a_id, npc_b_id)` since an NPC pair could plausibly hold more than one simultaneous relationship type in principle (e.g., `employer` + `mentor`) — confirm this reading is correct before assuming a pair can only have one row (Open Question 3).
- `engine/shared_types/npc_npc_relationship.gd` (`class_name NpcNpcRelationship extends RefCounted`) — fields exactly mirroring §5.4, plus `to_dict()`/`from_dict()` per the standard shared-type serialisation rule (`docs/coding_conventions.md` §7.2, line ~1224: booleans → 0/1, `from_dict` uses `.get(key, default)`).
- `engine/subsystems/generation/npcs/relationship_generator.gd` (`class_name RelationshipGenerator extends RefCounted`, mirroring `NpcPersonalityGenerator`'s shape and the non-autoload service pattern at `docs/coding_conventions.md` line ~3892): implements §5.3's full procedure — sociability banding read from an already-generated `NpcPersonality`'s axes (`Expressiveness >= 7 AND Civility >= 7` → 4-5 relationships; `Expressiveness <= 3 OR Stress Reactivity >= 8` → 1-2; else 2-3 — evaluate in that order, first match wins, exactly as GDD §5.3 step 1b specifies), the type-selection bias rules (same faction / rival faction / same role / same district / class-level gap, §5.3 step 1c — same-faction and rival-faction biases require a new query against `faction_memberships`/`factions`, see the "What already exists" note above; this is new query code, not a stub), the per-relationship field assignment (type/strength/mutual/public/note, §5.3 step 1d), and the §5.3 step 2 network validation pass (every faction leader ↔ every other faction leader; settlement ruler ↔ major POI operators; thieves' guild master ≥1 co-conspirator + ≥1 enemy; no isolated NPC). **The validation pass in §5.3 step 2 is settlement-wide, not per-NPC** — it needs the full roster of Tier B+ NPCs for a settlement generated before it can run coherently (relevant for RG-3's wiring decision, not this session's internal logic, but design the generator's public API to accept a roster/settlement scope rather than a single NPC so RG-3 doesn't need to restructure it).
- Repository CRUD in `CampaignRepository` (`engine/autoloads/campaign_repository.gd`): follow the `get_<thing>`/`list_<things>`/`save_<thing>`/`delete_<thing>`/`count_<things>` naming convention (`docs/coding_conventions.md` §6.2, line ~750) — at minimum `save_npc_npc_relationship(data: Dictionary) -> bool` (upsert), `list_npc_npc_relationships_for(npc_id: String) -> Array`, `list_npc_npc_relationships_for_settlement(settlement_id: String) -> Array` (for the RG-1 step-2 validation pass and later RG-3/RG-4 consumers). Add `npc_npc_relationships` to the purge cascade: it is NOT campaign-scoped by a direct column, so it belongs in the `_via` chain (near line 5408's `faction_memberships` precedent) keyed off `characters` — verify the exact `_via()` helper signature in the file before writing the entry (a relationship row references TWO character ids, `npc_a_id` and `npc_b_id`; confirm whether the existing `_via()` helper supports an OR-style two-column reference or whether the delete needs a custom clause — this is worth a quick look at how any other two-FK table, if one exists, is purged, before assuming `_via()`'s single-column signature suffices).

**Acceptance bar.** Migration applies clean on a fresh DB and on a copy of an existing campaign DB; `NpcNpcRelationship` round-trips through `to_dict()`/`from_dict()`; golden tests over §5.3's sociability bands (an NPC with Expressiveness 8/Civility 8 gets 4-5 relationships; an NPC with Expressiveness 2 gets 1-2; a mid-range NPC gets 2-3); type-selection bias tests (same-faction NPCs bias toward friend/employer/patron/co-conspirator; rival-faction NPCs bias toward rival/enemy — requires test fixtures with real `faction_memberships` rows); network validation catches and fixes (or at minimum flags) an isolated NPC and a faction-leader pair missing a cross-relationship; determinism (same seed + same roster → identical relationship set); purge cascade deletes `npc_npc_relationships` rows with the campaign; net-zero suite.

**Paste-ready prompt:**
> Implement session RG-1 of the NPC Personality Generators arc per `generation/gdd-npc-personality.md` §5 (all subsections) and `docs/handoff-npc-personality-generators.md` §3. Run the Build Session Protocol first (`acks-build-log --for-task "npc relationship generator"`, conventions navigator for-task "migration, shared types, repository CRUD, purge cascade"). Verify 186 is still the latest migration before numbering yours; write `db/migrations/187_npc_npc_relationships.sql` per the handoff's column list (ten relationship types from GDD §5.2, no assumed UNIQUE constraint on the pair — confirm against the handoff's Open Question 3 first). Add `NpcNpcRelationship` to `engine/shared_types/`. Build `RelationshipGenerator` in `engine/subsystems/generation/npcs/relationship_generator.gd` implementing GDD §5.3's full procedure (sociability banding from an NpcPersonality's axes, faction-aware type-selection biases querying real `faction_memberships`/`factions` rows, and the settlement-wide network validation pass) — design its public API to accept a full NPC roster/settlement scope, not a single NPC, since validation needs the whole settlement. Add repository CRUD to `CampaignRepository` following the `get_/list_/save_/delete_/count_` naming convention, and register the new table in the purge cascade (`_via` chain, keyed off `characters`; the row references two character ids — check how any other two-FK table is purged before assuming a single-column `_via()` call works). Do NOT name the shared type `NpcRelationship` — that name is reserved by `gdd-npc-dialogue.md`'s future PC↔NPC type; use `NpcNpcRelationship`. Tests: sociability bands, faction-aware bias selection, network validation (isolated-NPC catch, faction-leader cross-relationship), determinism, migration on fresh + existing DB, purge cascade. Net-zero suite, then `build_log.md` entry, flagging anything genuinely ambiguous in §5.3 rather than guessing.

---

## 4. Session RG-2 — Knowledge table, type, assigner, and rumor-pool linkage

**Goal.** Give every resident NPC a generated knowledge set per GDD §6, unblocking `gdd-quest-rumor-system.md`'s own distribution logic which is currently only implementable for the hardcoded `'local'` category.

**GDD refs.** `generation/gdd-npc-personality.md` §6.1 (purpose, lines 387-389), §6.2 (nine knowledge categories, lines 391-403), §6.3 (assignment procedure — baseline/role/relationship/rumor, lines 405-436), §6.4 (data shape, lines 438-450); `generation/gdd-quest-rumor-system.md` §2.2 (`RumorEntry` shape, lines ~40-87 per the read excerpt — get the exact line range at session start, this handoff quoted an excerpt starting at its file's line 60), §2.4 (distribution rule, lines 113-131, quoted in full in the "What already exists" section above).

**Files.**
- Migration `db/migrations/188_npc_knowledge_entries.sql` (verify 187 landed from RG-1 first; if RG-1 and RG-2 ran out of order or in parallel, renumber to avoid collision — coordinate migration numbers across the two sessions before either lands). Table `npc_knowledge_entries`, columns per GDD §6.4 verbatim: `npc_id TEXT NOT NULL REFERENCES characters(id)`, `category TEXT NOT NULL CHECK(category IN ('local','professional','political','criminal','religious','military','dungeon','personal','historical'))` (the nine categories from §6.2 — this exact set is what `gdd-quest-rumor-system.md` line 72-74 already quotes verbatim as its own dependency, so the CHECK list must match precisely, letter for letter, including the underscore-free multi-word absence — there are none, all nine are single words), `fact TEXT NOT NULL`, `accuracy TEXT NOT NULL CHECK(accuracy IN ('true','partially_true','false','outdated'))`, `source TEXT NOT NULL DEFAULT ''`, `willingness_to_share TEXT NOT NULL CHECK(willingness_to_share IN ('freely','if_trusted','if_paid','never'))`, `shared_with_party INTEGER NOT NULL DEFAULT 0`. Note §6.4's `accuracy` enum (`true/partially_true/false/outdated`) is a **different vocabulary** than `gdd-quest-rumor-system.md` §2.3's rumor-accuracy enum (`true/exaggerated/misleading/false`) — these are two distinct concepts (an NPC's confidence in a personal fact vs. a rumor's narrative accuracy category) that happen to share the word "accuracy" and the value "true"/"false." Do not conflate the two enums or try to unify them; keep `npc_knowledge_entries.accuracy` exactly as GDD-personality §6.4 specs it.
- `engine/shared_types/knowledge_entry.gd` (`class_name KnowledgeEntry extends RefCounted`) — fields mirroring §6.4 exactly, `to_dict()`/`from_dict()` per the standard convention. (Confirm this name doesn't collide with anything else project-wide before landing — a quick grep for `class_name KnowledgeEntry` across `engine/shared_types/` turned up nothing as of 2026-07-07, but re-verify at session start since other concurrent work may have added something.)
- `engine/subsystems/generation/npcs/knowledge_assigner.gd` (`class_name KnowledgeAssigner extends RefCounted`, same non-autoload service pattern): implements §6.3 in full — baseline local knowledge scaled by tier (Tier C 3-5 facts, Tier B 8-12, Tier A 15+, §6.3 step 1), role-based knowledge grants per category (merchant → professional/local/political; guard → military/local/criminal; priest → religious/historical/personal, §6.3 step 2 — this will need a role→category-weighting data table, likely a new JSON file in `data/templates/` alongside the existing `role_defaults.json`, or an extension of that same file; decide and document which), relationship-based knowledge (1-3 personal knowledge items per `NpcNpcRelationship` edge from RG-1, with `co-conspirator` granting shared criminal knowledge and `rival` granting knowledge of the rival's weaknesses, §6.3 step 3 — **this step is a hard dependency on RG-1's table existing and being populated first**, confirm RG-1 landed before starting this piece), and rumor-pool knowledge (2-5 rumors per NPC, motivation-influenced selection — wealth-motivated NPCs bias toward treasure rumors, faith-motivated toward religious rumors, revenge-motivated toward rumors about their enemy, §6.3 step 4). **The rumor-pool step is where this session touches `gdd-quest-rumor-system.md` directly** — confirm whether rumor generation itself (the `RumorEntry` rows) is this session's responsibility or a separate, already-existing system that `KnowledgeAssigner` merely *selects from*; per the "concurrently being spec'd" framing in this handoff's originating brief, the rumor-pool generation mechanics may be under active design elsewhere — **do not build rumor generation in this session; only build the selection/assignment logic that picks from an assumed-existing rumor pool**, and flag the interface boundary explicitly in `build_log.md` (see Open Question 5).
- Repository CRUD in `CampaignRepository`: `save_knowledge_entry(data: Dictionary) -> bool`, `list_knowledge_entries_for(npc_id: String) -> Array`, `list_knowledge_entries_by_category(npc_id: String, category: String) -> Array` (for the dialogue system's future filtered-context assembly per GDD-personality §9.2), `count_knowledge_entries(npc_id: String) -> int`. Register `npc_knowledge_entries` in the purge cascade `_via` chain, keyed off `characters` (single-FK this time — simpler than RG-1's two-FK case).

**Acceptance bar.** Migration applies clean fresh + existing DB; `KnowledgeEntry` round-trips; golden tests over §6.3's tier-scaled baseline counts (Tier C gets 3-5, Tier B 8-12, Tier A 15+); role-based category grants match the §6.3 examples (merchant/guard/priest); relationship-based knowledge correctly reads `npc_npc_relationships` rows from RG-1 and grants co-conspirator/rival-flavored knowledge; rumor-pool selection respects motivation biasing; `willingness_to_share` values are exactly the four GDD-specced enum values; determinism; purge cascade; net-zero suite. **Do not** ship rumor-row generation in this session — only selection logic against an assumed rumor pool interface (document the assumed interface shape in `build_log.md` so the quest-rumor build session can conform to it or flag a mismatch).

**Paste-ready prompt:**
> Implement session RG-2 of the NPC Personality Generators arc per `generation/gdd-npc-personality.md` §6 (all subsections) and `docs/handoff-npc-personality-generators.md` §4. Run the Build Session Protocol first. Check whether RG-1's migration 187 has landed (relationship generation is a dependency for step 3 of this session); if not landed, coordinate before numbering — do not silently take 187 if RG-1 already claimed it. Write the `npc_knowledge_entries` migration per the handoff's exact column list (nine categories matching `gdd-quest-rumor-system.md`'s own quoted dependency letter-for-letter; note the `accuracy` enum here is `true/partially_true/false/outdated`, a DIFFERENT vocabulary than the rumor system's own `true/exaggerated/misleading/false` — do not conflate them). Add `KnowledgeEntry` to `engine/shared_types/`. Build `KnowledgeAssigner` in `engine/subsystems/generation/npcs/knowledge_assigner.gd` implementing GDD §6.3's four-step procedure (tier-scaled baseline, role-based grants — likely needs a new or extended `data/templates/role_defaults.json`-style table, decide and document which — relationship-based grants reading RG-1's `npc_npc_relationships` table, and motivation-biased rumor-pool selection). Do NOT build rumor-row generation mechanics in this session — only the selection logic that picks from an assumed-existing rumor pool; document the assumed interface in `build_log.md` since `gdd-quest-rumor-system.md`'s own generation mechanics may be under separate concurrent design. Add repository CRUD to `CampaignRepository` and register the purge cascade. Tests: tier-scaled baselines, role grants, relationship-based grants (depends on RG-1 data), rumor-pool motivation biasing, determinism, migration, purge cascade. Net-zero suite; `build_log.md` entry; flag ambiguities rather than guessing.

---

## 5. Session RG-3 — Wire into settlement stocking and NPC materialization

**Goal.** Make relationship and knowledge generation actually fire when NPCs are created, at the right granularity (settlement-wide for the network validation pass, not per-POI).

**GDD refs.** §5.1 (generation timing — "settlement stocking time" + "during play"), §10.1 (generation timing table — settlement stocking = Tier B batch, henchman hiring = Tier B on hire, tier promotion = fill in missing fields).

**Files.**
- `engine/subsystems/settlements/baseline_npc_stocker.gd`: `stock_poi()` (line 115) currently fires per-POI off `EventBus.poi_emerged`/emits `EventBus.poi_stocked` per-POI (`event_bus.gd:2032`/`:2037`). **§5.3's network validation (every faction leader ↔ every other faction leader; no isolated NPC) is a settlement-wide invariant** — it cannot be correctly evaluated from a single POI's NPC roster, since faction leaders and the settlement ruler may be stocked across multiple different POIs. Verify whether a settlement-level "all POIs stocked" completion signal already exists (a plausible candidate name would be something like `settlement_stocking_completed` — **this signal was not confirmed to exist as of the 2026-07-07 codebase read; check `event_bus.gd`'s full 346-signal list before assuming it's there or absent**). If it doesn't exist, this session needs to add one (past-tense per convention) fired by whatever orchestrates multi-POI settlement stocking (identify that orchestrator first — `baseline_npc_stocker.gd` only shows the per-POI path in the parts read for this handoff; there may be a higher-level settlement-stocking driver not yet located). Relationship generation (RG-1) and the knowledge assigner's relationship-dependent step (RG-2 step 3) both belong on this settlement-wide completion path, not on `poi_stocked`.
- `engine/subsystems/characters/classed_npc_builder.gd`: add `generate_relationships`/`generate_knowledge` opts (default matching whatever `generate_personality`'s default is, likely `true`) to `build_classed_npc()`, following the exact pattern of the existing `generate_personality` opt at lines 220-234 — but note relationship generation for a single hired/interviewed NPC (the henchman-hiring path, GDD §10.1) is **not** a settlement-wide network validation case; it's closer to "this one new NPC gains a few relationships with existing NPCs it would plausibly know" — the generator's public API designed in RG-1 to accept a roster/scope should also support a single-NPC-against-existing-roster mode. Confirm this dual-mode requirement doesn't already fall out of RG-1's API design; if it doesn't, this session needs to extend `RelationshipGenerator`, which pushes scope back onto RG-1's session — flag this coordination need explicitly at RG-1 time, don't discover it cold in RG-3.
- Tier promotion (GDD §4.2's closing paragraph, and §10.1's tier-promotion row: "Sample the defaulted axes; fill in missing fields"): when a Tier C NPC is promoted to Tier B (the party keeps interacting with them), relationships and knowledge need to be generated retroactively for the first time (a Tier C NPC per GDD §7.2 has no relationships/knowledge arrays at all, only a flat `knowledge: Array[string]` of 3-5 plain-text facts — different shape than the full `KnowledgeEntry` array). Locate the existing tier-promotion code path (not identified in this handoff's research pass — locate it at session start, likely near wherever `NPCPersonalityCompact`/Tier-C records get upgraded) and wire both generators in.

**Acceptance bar.** A freshly settlement-stocked campaign produces a fully validated relationship network (per RG-1's acceptance bar) and complete knowledge sets (per RG-2's) across the WHOLE settlement, not per-POI; a henchman hired mid-campaign gains a plausible small relationship set against the existing roster without re-running settlement-wide validation; a Tier C→B promotion backfills relationships/knowledge for the promoted NPC; net-zero suite; `build_log.md` entry documenting the settlement-completion signal (new or found) and the promotion hookup point.

**Paste-ready prompt:**
> Implement session RG-3 of the NPC Personality Generators arc per `docs/handoff-npc-personality-generators.md` §5, building on RG-1 and RG-2 (confirm both have landed first — this session hard-depends on `npc_npc_relationships` and `npc_knowledge_entries` existing). Run the Build Session Protocol. First, locate the settlement-stocking orchestration: confirm whether a settlement-wide "all POIs stocked" completion signal exists in `engine/autoloads/event_bus.gd` (grep its ~346 signals; do not assume a name, find or add it) — GDD §5.3's network validation (faction-leader cross-relationships, no isolated NPC) is a settlement-wide invariant that cannot run correctly per-POI. Wire `RelationshipGenerator` and `KnowledgeAssigner`'s relationship-dependent step onto that settlement-wide completion, not onto the existing per-POI `poi_stocked` signal. Add `generate_relationships`/`generate_knowledge` opts to `ClassedNpcBuilder.build_classed_npc()` mirroring the existing `generate_personality` opt (lines 220-234) — for this single-NPC-against-existing-roster case (henchman hiring), confirm `RelationshipGenerator`'s API from RG-1 supports operating against a partial/existing roster, not just a fresh full-settlement generation; extend it if it doesn't, and note in `build_log.md` that this requirement should have been anticipated at RG-1 time. Locate the Tier C→B promotion code path and wire in retroactive relationship/knowledge generation for a promoted NPC. Tests: settlement-wide validation actually validates across POIs (not per-POI), henchman-hire relationship generation against existing roster, promotion backfill. Net-zero suite; `build_log.md` entry.

---

## 6. Session RG-4 — Feed `DialogueContext`

**Goal.** Close the loop into the dialogue system, which already names the consumer slots it expects.

**GDD refs.** `generation/gdd-npc-dialogue.md` §4.3 (`DialogueContext` struct, lines 165-191 — specifically `personality: Dictionary` at line 182 and `hooks.rumor_pool_ids`/`hooks.knowledge_entries` at line 185), §9.1 (knowledge disclosure gating by `willingness_to_share`, lines 502-506), §9.2 (rumor acquisition via `ask_rumor`, lines 508-510, cross-referencing `gdd-quest-rumor-system.md §2.5`); `generation/gdd-npc-personality.md` §9.2 (`NPC Context Package` runtime assembly — `relevant_knowledge`/`relevant_relationships`/`motivation_hooks` fields, lines 774-784).

**Files.**
- Wherever `DialogueContext` gets assembled (per §4.2 of the dialogue GDD, "every existing 'talk-shaped' surface routes into `DialogueSession.begin()` with a typed context" — locate the actual assembler at session start; not yet built as of the dialogue GDD's own Draft status, so this may be greenfield or may already exist as a stub by the time this session runs — check `build_log.md` for a dialogue-system session before assuming). Populate:
  - `personality: Dictionary` — from `characters.personality` (already-existing JSON column, already populated by the core layer since 2026-06-14 — this part needs no new generation, just a read-and-attach).
  - `hooks.knowledge_entries: Array` — from RG-2's `list_knowledge_entries_for(npc_id)`, filtered per GDD-dialogue §9.1's gating (though the actual attitude-check gating logic belongs to the dialogue adjudicator, not this session — this session's job is surfacing the raw entries with their `willingness_to_share` tags intact for the adjudicator to filter, per GDD-personality §9.2's "The assembler pulls only relevant data" principle).
  - `hooks.rumor_pool_ids: Array` — from whatever RG-2 wired as the NPC's selected rumor pool (confirm the exact field/table RG-2 landed for this, since RG-2's session explicitly deferred rumor-row generation itself — this session may only be able to surface rumor IDs if the rumor system's own generation has also landed by this point; if not, this field stays empty with a documented TODO, do not stub fake rumor ids).
  - `relevant_relationships` (per GDD-personality §9.2's `NPC Context Package`, not literally named in the dialogue GDD's §4.3 struct as quoted — check whether the dialogue GDD's struct needs an explicit field added for this, or whether relationships surface some other way; this is a possible gap between the two GDDs' specs, worth a quick cross-check rather than assuming one subsumes the other).
- Confirm this session does NOT touch or rename `NpcRelationship` — that identifier is reserved (per §1's collision warning above) for the dialogue system's own future PC↔NPC type; this session only reads from RG-1's `NpcNpcRelationship` table to populate a *value*, not to define or extend that type.

**Acceptance bar.** A live dialogue session (even against the mock LLM / template provider) has a populated `personality`, `hooks.knowledge_entries`, and `hooks.rumor_pool_ids` (or a documented empty-with-TODO state if the rumor system hasn't landed yet) sourced from real generated data, not placeholders; `ask_question` can actually gate on a real `willingness_to_share` value from a generated `KnowledgeEntry`; net-zero suite; `build_log.md` entry.

**Paste-ready prompt:**
> Implement session RG-4 of the NPC Personality Generators arc per `docs/handoff-npc-personality-generators.md` §6, building on RG-1/RG-2/RG-3. Run the Build Session Protocol. Locate where `DialogueContext` (per `generation/gdd-npc-dialogue.md` §4.3) gets assembled — check `build_log.md` first for whether a dialogue-system build session has already scaffolded this struct; it may be greenfield. Populate `personality` (read from the existing `characters.personality` column, no new generation needed), `hooks.knowledge_entries` (from RG-2's `list_knowledge_entries_for`, surfacing raw entries with `willingness_to_share` intact — do not apply attitude-gating here, that's the dialogue adjudicator's job), and `hooks.rumor_pool_ids` (from whatever rumor-pool field RG-2 landed; if the rumor-generation system itself hasn't shipped, leave this empty with a documented TODO rather than stubbing fake ids). Check whether `gdd-npc-personality.md` §9.2's `relevant_relationships` field needs an explicit home in the `DialogueContext` struct or whether it's out of scope for this session — flag it if genuinely unclear rather than guessing. Do NOT rename, extend, or otherwise touch the `NpcRelationship` identifier reserved for the dialogue system's own PC↔NPC type — this session only reads FROM `NpcNpcRelationship` (RG-1) to populate a context value. Tests: a live/mock dialogue session shows populated personality/knowledge/rumor-pool hooks from real generated data; `ask_question` gates correctly on a real `willingness_to_share`. Net-zero suite; `build_log.md` entry.

---

## 7. After RG-4

- Confirm the §2 global definition of done end-to-end on a mixed campaign (multiple settlements, at least one with a thieves' guild and a rival faction pair).
- Update `docs/coding_conventions.md`: the `npc_npc_relationships`/`npc_knowledge_entries` table shapes, the settlement-wide-vs-per-POI generation-scope distinction (a pattern likely to recur for any future settlement-wide validation pass), and the resolved naming for `NpcNpcRelationship` vs. the dialogue system's `NpcRelationship`.
- Record in the build log, for the quest-rumor build session's benefit: the exact interface RG-2 assumed for "an existing rumor pool" (table name, fields, whatever RG-2 actually wired), so that session can conform to it or flag a mismatch explicitly rather than discovering divergence after the fact.
- Forward work (do NOT build from this handoff): the rumor-row generation mechanics themselves (`gdd-quest-rumor-system.md`'s own build, evidently in concurrent design as of this handoff's writing); the dialogue system's own `NpcRelationship` (PC↔NPC attitude/ledger) and `NpcMemory`/`NpcMemoryStore` types (`gdd-npc-dialogue.md` §8), which are a related but distinct future build; live-LLM personality summary generation (still mock-only per the GDD's §13 revision history).

---

## 8. Quick interface index (verify against code before use — re-check line numbers, this handoff's citations are frozen at the 2026-07-07 read and will drift as sessions land)

- Existing personality core: `engine/subsystems/generation/npcs/{axis_sampler,npc_personality_generator,personality_axes,personality_mock}.gd`; `engine/shared_types/npc_personality.gd` (`NpcPersonality`, no relationship/knowledge fields).
- New files this handoff specs: `engine/subsystems/generation/npcs/relationship_generator.gd` (`RelationshipGenerator`, RG-1), `engine/subsystems/generation/npcs/knowledge_assigner.gd` (`KnowledgeAssigner`, RG-2); `engine/shared_types/npc_npc_relationship.gd` (`NpcNpcRelationship`, RG-1), `engine/shared_types/knowledge_entry.gd` (`KnowledgeEntry`, RG-2).
- Migrations: `db/migrations/`, latest verified 2026-07-07 is `186_armies_provenance.sql` — RG-1 takes `187_npc_npc_relationships.sql`, RG-2 takes the next free number (coordinate if run out of order).
- Repositories: `CampaignRepository` is an **autoload** at `engine/autoloads/campaign_repository.gd` (not under a `subsystems/persistence/` path — verified, a prior assumption to that effect was wrong). Purge cascade: `_SCOPE_DIRECT_CAMPAIGN` array starts line 5329 (36+ entries, e.g. `"factions"` at line 5335); `_via` chain starts ~line 5390, `faction_memberships` precedent at line 5408 (`_via("faction_id", "factions")`); `settlement_pois` precedent at line 5410 (`_via("settlement_id", "settlement_entrances")`).
- Consumers: `NpcRulerGenerator` (`engine/subsystems/domains/npc_ruler_generator.gd`, personality attach lines 133-137, `StrategicDispositionBuilder` call lines 161-165 — confirms §8 already shipped); `BaselineNpcStocker` (`engine/subsystems/settlements/baseline_npc_stocker.gd`, `stock_poi()` line 115, personality builds lines 208/237, `poi_stocked` emit ~line 244); `ClassedNpcBuilder` (`engine/subsystems/characters/classed_npc_builder.gd`, personality attach lines 220-234).
- Signals: `engine/autoloads/event_bus.gd` — `poi_emerged` line 2032, `poi_stocked` line 2037 (346 total signals in the file as of this read; grep before assuming any name exists or doesn't).
- Existing faction tables (unwired into this pipeline today): `factions` (`db/schema.sql:1465`), `faction_memberships` (`db/schema.sql:1479`), both from `db/migrations/025_reputation_system.sql`.
- Already-shipped, adjacent, and NOT to be rebuilt: `engine/shared_types/strategic_disposition.gd`, `engine/shared_types/ruler_profile.gd`, `engine/subsystems/realm_ai/strategic_disposition_builder.gd` (GDD §8; supersedes GDD §11.1's old file-placement plan) — its docstring names the RG-1 gap as an active blocker for its own revenge-bump term.
- Dialogue-system consumer (Draft status, may be partially built by the time RG-4 runs): `generation/gdd-npc-dialogue.md` §4.3 `DialogueContext` (lines 165-191); reserves the identifier `NpcRelationship` for its own PC↔NPC type (line 180) — DO NOT reuse for RG-1's NPC↔NPC type, which is `NpcNpcRelationship`.
- Quest-rumor consumer (pre-existing GDD, Draft, last updated 2026-03-28, NOT newly concurrent): `generation/gdd-quest-rumor-system.md` — `RumorEntry.knowledge_category` (line 72, quotes GDD-personality §6.2's nine categories verbatim), §2.4 distribution rule (lines 118-131, requires a populated `knowledge_category` per NPC — currently only satisfiable for the hardcoded `'local'` fallback).
- Tests: `tests/test_npc_personality.gd` (19 tests, core-only coverage confirmed, no relationship/knowledge tests exist yet); registration pattern is a two-edit flat-array idiom in `tests/test_runner.gd` (an `@onready var` node reference + inclusion in the `run()` iteration array — both edits carry a dated comment, e.g. `# 2026-06-14 — NPC personality core layer...`; a new relationship/knowledge test suite follows the identical two-edit pattern).

---

## 9. Open Questions / Rulings Needed from Jedidiah

These are gaps or ambiguities in the existing §5/§6 design (or its intersection with adjacent GDDs) that a build session cannot resolve by inference without risking a wrong guess. Do not start the affected session-piece until answered, or proceed with the documented default only if explicitly told to.

1. **Naming: is `NpcNpcRelationship` (this handoff's placeholder) acceptable, or is there a preferred name for the NPC↔NPC social-network type?** The collision to avoid is concrete — `gdd-npc-dialogue.md` §4.3 line 180 already reserves `NpcRelationship` for a PC↔NPC attitude/ledger type. Alternatives considered but not chosen unilaterally: `SocialRelationship`, `NpcSocialTie`, `RelationshipEdge`. This affects the class name, file name, and table name (`npc_npc_relationships`) landed in RG-1's migration — a rename after the fact is a migration + repository-method rename, so better to settle before RG-1 starts.

2. **Directionality encoding for the `unrequited` relationship type.** GDD §5.4's data shape has `npc_a_id`/`npc_b_id`/`mutual: bool` but no field distinguishing "who is the admirer" for a one-sided (`unrequited`) relationship. Is `mutual=0` on an `unrequited` row sufficient (with a fixed convention that `npc_a_id` is always the admirer), or does the schema need an explicit marker? This is a real gap in §5.4 as written — the GDD doesn't address it.

3. **Can an NPC pair hold more than one simultaneous relationship-type row?** E.g., could two NPCs be both `employer` and `mentor` at once? GDD §5.2/§5.4 lists types as a flat enumeration and never states whether `(npc_a_id, npc_b_id)` is unique. RG-1's draft migration assumes NO uniqueness constraint (multiple rows per pair allowed) as the more conservative reading, but this should be confirmed — a wrong guess here is a schema change later (adding or removing a UNIQUE constraint on a live table is more disruptive than getting it right at RG-1).

4. **Should RG-1 itself wire the `StrategicDisposition` revenge-bump term (§8.3), or only unblock it for a separate follow-up session?** `strategic_disposition_builder.gd`'s docstring names this as dormant, waiting on relationship data. RG-1 will make the data available (an `enemy`-typed `NpcNpcRelationship` row identifies a revenge subject), but actually wiring the `+0.5` bump into `strategic_disposition_builder.gd` touches a different subsystem (`realm_ai/`) that this handoff's scope was framed around NPC personality generation, not ruler AI. Recommend treating this as a small, separate follow-up session once RG-1 lands (flagged `[NEEDS-OPUS-REVIEW]` per CLAUDE.md's cross-subsystem-integration guidance) rather than folding it into RG-1 — but confirm this scoping choice rather than assuming it.

5. **What is the actual state of rumor-row generation mechanics, and what interface should `KnowledgeAssigner` (RG-2) assume for "an existing rumor pool"?** This handoff's originating brief said the quest-rumor system is "being spec'd concurrently" — but the investigation found `generation/gdd-quest-rumor-system.md` is a substantial, pre-existing GDD (951 lines, last updated 2026-03-28, well before the personality GDD's own major rework). It's unclear whether: (a) that GDD is stale and a NEW concurrent quest-rumor design effort is happening elsewhere not yet visible in the repo, (b) that GDD is simply the current, not-yet-implemented design and RG-2 should build directly against its §2.2 `RumorEntry` shape as the assumed interface, or (c) something in between. RG-2's session-prompt above defers rumor-row generation and only builds selection logic — but "selection logic against an assumed interface" needs that interface's shape confirmed before RG-2 starts, or the session will guess a shape that later needs reconciling. **This is the single highest-value question to resolve before starting RG-2** — everything else in this handoff can proceed with the documented defaults, but RG-2's rumor-pool step genuinely cannot proceed correctly without this answer.

6. **Does `gdd-npc-dialogue.md`'s `DialogueContext` struct need an explicit field added for `relevant_relationships`** (named in GDD-personality §9.2's `NPC Context Package` but not obviously present in the dialogue GDD's §4.3 struct as quoted in this handoff)? This may be a genuine spec gap between the two GDDs, or `relevant_relationships` may be intended to fold into the existing `hooks` dictionary under a key this handoff's research pass didn't surface (the dialogue GDD is 1,264 lines; only §4.3 and §9.1 were read closely for this handoff). Worth a full read of the dialogue GDD before RG-4 starts, specifically checking for any relationship-surfacing field elsewhere in the struct or its surrounding sections.

7. **Role→knowledge-category weighting table: new file or extend `role_defaults.json`?** GDD §6.3 step 2's role-based knowledge grants (merchant → professional/local/political, etc.) need a data table. `data/templates/role_defaults.json` already exists (used for default motivation per role, per GDD §4.2/§10.1) — RG-2 could extend that file with a knowledge-category-weights block, or create a sibling file (e.g., `data/templates/role_knowledge_categories.json`). This is a low-stakes engineering call Sonnet can likely make independently (the CLAUDE.md Document Authority rules mark `generation/` GDDs and their downstream implementation as things Claude Code MAY make engineering calls on) — listed here mainly so RG-2's build session doesn't treat it as blocking; default to extending the existing file unless a reason to split emerges during implementation.
